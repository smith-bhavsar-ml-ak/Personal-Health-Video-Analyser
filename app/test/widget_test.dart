import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder smoke test', (WidgetTester tester) async {
    // Placeholder — PHVA requires a running backend to boot.
    // Integration tests live in test/integration/.
    expect(true, isTrue);
  });
}
